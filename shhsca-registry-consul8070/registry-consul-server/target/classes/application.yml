server:
  port: 8070

spring:
  application:
    name: shhsca-registry-consul-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
